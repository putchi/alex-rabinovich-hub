# ── Sanity CMS ────────────────────────────────────────────────────────────────
# Sanity project ID (found at manage.sanity.io)
VITE_SANITY_PROJECT_ID=your_project_id

# Sanity dataset name
VITE_SANITY_DATASET=production

# Read-only Viewer token (required if dataset is private)
# Create at: manage.sanity.io → project → API → Tokens → Add (Viewer role)
VITE_SANITY_TOKEN=

# ── Static meta tags (used in index.html before React loads) ──────────────────
# These cannot come from Sanity without causing a browser tab flicker.
VITE_OWNER_NAME=
VITE_SITE_TITLE=
VITE_SITE_DESCRIPTION=

# ── Deprecated (content now lives in Sanity) ──────────────────────────────────
# VITE_OWNER_TITLE=
# VITE_LINKEDIN_URL=
# VITE_GITHUB_URL=
